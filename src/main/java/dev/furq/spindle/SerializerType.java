package dev.furq.spindle;

/**
 * Enum representing different types of string serializers.
 */
public enum SerializerType {
    /** Legacy serializer type. */
    LEGACY,
    /** MiniMessage serializer type. */
    MINIMESSAGE,
    /** No serialization. */
    NONE
}
